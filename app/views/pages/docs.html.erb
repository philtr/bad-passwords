<% content_for :title, "Bad Passwords Docs" %>

<%= render "pages/navigation" %>

<h1>Bad Passwords Docs</h1>

<p>Bad Passwords provides registration, login, token validation, and logout endpoints for delegated password verification backed by a remote Argon2 hash.</p>

<%= render "api_docs" %>

<hr>

<%= render "pages/footer" %>
